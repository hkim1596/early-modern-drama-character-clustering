pandas
openpyxl
rapidfuzz
tqdm
numpy
scikit-learn
plotly
umap-learn
hdbscan
torch
transformers>=4.45,<4.50   # newer (5.x) breaks gte-Qwen2's custom modeling code
# Recommended on GPU:
# accelerate
# flash-attn   (install with: pip install flash-attn --no-build-isolation)
